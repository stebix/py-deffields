# poject description

This project is concerned with the implementation of volumetric 3D deformation fields and noise patterns
as a preprocessing stage for volumetric image warping and augmentation.

# development environment

The project development environment is strictly `uv` and Linux Debian 10 with `bash`.
We utilize a project-specific virtual environment ot isolate Python and project dependencies from 
system and other influences. 